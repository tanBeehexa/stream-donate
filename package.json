{
  "scripts": {
    "start": "node index.js",
    "deploy": "git push && git push heroku main"
  },
  "dependencies": {
    "express": "^4.18.1",
    "socket.io": "^4.5.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
