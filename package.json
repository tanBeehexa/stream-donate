{
  "scripts": {
    "local": "nodemon index.js",
    "start": "node index.js",
    "deploy": "git push && git push heroku main",
    "log": "heroku logs --tail"
  },
  "dependencies": {
    "express": "^4.18.1",
    "socket.io": "^4.5.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
