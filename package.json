{
  "scripts": {
    "local": "nodemon index.js",
    "start": "node index.js",
    "deploy": "git push && git push heroku main",
    "log": "heroku logs --tail"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cors-anywhere": "^0.4.4",
    "express": "^4.18.1",
    "socket.io": "^4.5.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
